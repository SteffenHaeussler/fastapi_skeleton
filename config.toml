VERSION = "0.1.0"

[DEV]
CONFIG_NAME = "dev"
DEBUG = false
# CORS is disabled unless enabled per deployment:
# [DEV.cors]
# enabled = true
# allow_origins = ["https://example.com"]
# allow_methods = ["GET", "POST"]
# allow_headers = ["*"]
# allow_credentials = false
# Optional observability hooks are disabled unless enabled per deployment:
# [DEV.observability.prometheus]
# enabled = true
# path = "/metrics"
# [DEV.observability.tracing]
# enabled = true
# service_name = "fastapi_skeleton"

[PROD]
CONFIG_NAME = "prod"
DEBUG = false

[STAGE]
CONFIG_NAME = "stage"
DEBUG = false

[TEST]
CONFIG_NAME = "test"
DEBUG = true
